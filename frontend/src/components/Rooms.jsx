import "./Rooms.css";
import { toast } from "react-toastify";
import 'react-toastify/dist/ReactToastify.css';
const Rooms = () => {
  return (
      <>
        {toast.error("Fetch from DB")}
      </>
  );
};

export default Rooms;
